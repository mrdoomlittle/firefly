command and control approch, its to be controled by sending operations over shm,sock, etc

well that should be a way, why?? because compile time its easyer to debug somthings without needing to compile or relink file objects,
so the engine is to be pre compiled and the game will interface with the engine as such.
