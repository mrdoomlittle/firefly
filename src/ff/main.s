	.region text
abort:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_mem_alloc:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_mem_free:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_mem_read:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_mem_write:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_sched_init:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
ffly_resv_init:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
main:
pushq %rbp
movq %rbp, %rsp
movq %rsp, %rbp
popq %rbp
ret
.endof
